<!DOCTYPE html>

<h2> Edit Your Record </h2>
<div id="form">
<%= simple_form_for @records, method: :patch do |f| %>
  <%= f.input :title %>
  <%= f.input :amount %>
  <%= f.input :created_at, :label => "Date" %>
  <%= f.submit 'Submit' %>
<% end %>
</div>